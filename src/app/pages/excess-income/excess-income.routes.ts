import { Routes } from '@angular/router';

/**
 * Роуты для модуля "Избыточный доход"
 *
 * Структура модуля:
 * - Основная страница избыточного дохода (ExcessIncomePageComponent)
 * - Модальные окна и поповеры для редактирования данных
 * - История изменений (комментарии, цены на группы, цены на товары, история продаж)
 * - Таблицы с данными по клиентам, подрядчикам, группам и товарам
 *
 * URL структура:
 * /excess-income-page - Основная страница модуля
 *
 * Модуль включает:
 * - Управление избыточным доходом по клиентам и товарным группам
 * - Редактирование СНД (средней нормы доходности) для клиентов
 * - История изменений цен и комментариев
 * - Фильтрацию и поиск по различным критериям
 * - Экспорт данных и аналитику
 *
 * Все компоненты загружаются через lazy loading для оптимизации производительности
 */
export const EXCESS_INCOME_ROUTES: Routes = [
	{
		path: '',
		loadComponent: async () =>
			import('./excess-income-page/excess-income-page.component').then(
				(c) => c.ExcessIncomePageComponent
			),
		title: 'Избыточный доход',
		data: {
			animation: 'animation',
		},
	},
];
